node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env.local
.env.development
.env.staging
.env.production
.env.*.local
!.env.example
firebase-applet-config.json

# Cloud Functions diarsipkan — tidak lagi digunakan (Spark plan, tanpa kartu kredit)
# Arsip tersimpan di functions/ untuk referensi. Hapus manual jika tidak diperlukan.
functions/lib/
functions/node_modules/

# === TAMBAHAN REMEDIATION 2026-05-31 ===
# Binary backup files — JANGAN DI-TRACK (besar, tidak punya value sebagai code)
*.zip
*.tar.gz
*.gz

# AI Studio / Firebase artifacts
metadata.json
firebase-applet-config.json

# Audit logs runtime — hanya ada untuk referensi dev, bukan source code
audit_logs/

# Secrets checklist yang mungkin berisi info sensitif
secrets-checklist.md
